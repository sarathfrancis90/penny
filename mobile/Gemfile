source "https://rubygems.org"

# Fastlane 2.239.0 still restricts rubyzip to vulnerable 2.x releases.
# Pin the official upstream Rubyzip 3 migration until a release includes it:
# https://github.com/fastlane/fastlane/pull/30176
# This is an unreleased upstream commit, not a local dependency override.
gem "fastlane", git: "https://github.com/fastlane/fastlane.git",
  ref: "3a4fc36716dec206b3f5441f19f0a5193733c828"

# Required by fastlane's `cocoapods` action in the iOS ship lane. Even though
# CocoaPods is pre-installed on the macos-14 runner via Homebrew, `bundle
# exec` isolates gem search to the Gemfile, so the action can't find the
# binary without this entry. Pinned to ~> 1.16 because Flutter 3.x Podfiles
# use features added after 1.10.
gem "cocoapods", "~> 1.16"

# Required by fastlane's `supply` action for Play Store uploads.
plugins_path = File.join(File.dirname(__FILE__), "fastlane", "Pluginfile")
eval_gemfile(plugins_path) if File.exist?(plugins_path)
