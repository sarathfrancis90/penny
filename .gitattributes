# Keep the Windows wrapper native on checkout while normalizing its index.
apps/android/gradlew.bat text eol=crlf
# Golden CSV has CRLF rows and LF inside quoted notes; preserve exact bytes.
packages/offline-contract/fixtures/*.csv -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
