# iOS metadata retention — 2026-09-14

The final isolated simulator run passed **33/33, zero skips**. Fifty real ordinary metadata edits now leave **two full metadata records (11,926 bytes) and one predecessor-pointer wrapper (394 bytes)**. The same workload on the unchanged repair runtime left **51 records (303,206 bytes) and 50 wrappers (19,583 bytes)**. Both runs installed the actual Android v4 fixture first, then verified all eight domains and the unchanged receipt ciphertext/raw bytes after reopening. These are measured metadata file sizes, not total filesystem allocation or performance results.

The existing root lease protects collection. Current, immediate predecessor and pending recovery state remain protected. Candidate metadata now holds a shared file lock, including candidates without receipts. A preservation-only prepass retains every receipt needed by reader-pinned historical records, including overlapping receipt sets. Actual removal still requires complete authenticated model/native image validation, protected file checks and pinned inode/hash rechecks. Unknown, unreadable or substituted entries remain in place; missing receipt descriptors never authorize deletion.

Successful publication invokes collection after its rollback catch and finalization. Cleanup errors or cancellation retain the committed result, with `storage-retention` OSLog counters/deferred-error output and an internal `CollectionReport`. Quarantine is preservation in place. Unreadable history and active pins may retain bytes indefinitely; they are outside the normal settled two-record bound.

Final coverage includes the five new retention methods, twelve durable-storage methods, nine receipt-foundation methods, selected live read/export/candidate pins, two repair paths, and the corrected hosted stale-incarnation method. FIFO metadata and receipt substitutions, overlapping reader pins, cancellation/injected cleanup failure, unchanged foreign replacement, pending/predecessor recovery and receipt-free candidates passed. The incarnation correction removes a redundant explicit replacement after eager legacy migration; the revision/epoch/stale-install assertions are unchanged.

Evidence is in [metadata-retention.json](metadata-retention.json), with source/binary pins, acceptance mapping and disk samples. Raw captures are `artifacts/offline/metadata-retention-ios/{before,after01,after02}`; final result is `apps/ios/.build/metadata-retention/After02.xcresult`. Before is an actual old-runtime run. After01's 22/22 result is preserved separately from the final FIFO/overlap corrections. Final source and executable hashes matched the pre-test capture.

Runtime: Debug arm64, Xcode 26.6, iOS Simulator 26.4.1 on owned F70. The simulator is shut down; normal demo data was untouched. Recovery used fresh store instances and interruption checkpoints within XCTest, not new forced process-death proof. Cleanup fault hooks are injections; no real full-volume/hardware-sync experiment or physical class-A protection claim. No UI, capacity, wire, cloud or performance expansion. Estimated work was approximately 14–16k tokens including confirmed review fixes, exceeding the original soft estimate.
