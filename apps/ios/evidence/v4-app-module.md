The actual PennyOffline app now links an internal `PennyV4` static module from the existing frame/logical sources and current stateless app validators. Only a synchronous, owned-source read-only summary is exposed. The app adapter retains the current 10,000-expense, 100-receipt and 8 MiB receipt limits. Legacy backup dispatch, UI, candidate installation and export have no new callers.

On the isolated F70 iPhone 16e / iOS 26.4.1 simulator, the final focused suite passed **6/6 methods, zero failures/skips**. It consumed the two real encrypted logical goldens, rejected both frame-only patterns and all 28 materialized frame negatives, and exercised all 70 shared logical cases through actual encryption/frame authentication. The 10,001-expense fixture was rejected at app admission. A separately rebuilt valid-AEAD/hash/transcript fixture containing the existing invalid-filter PNG reached and failed the native image validator. Read/close errors, cancellation during read/close and oversized reads returned no summary. Existing persisted file hashes and the all-domain snapshot remained unchanged across validation and rejection, including fresh-store reopen. The app bundle's ISC notice matched its pinned SHA-256.

Both iPhoneOS and simulator app builds compiled/linked. The live simulator app dylib contains upstream HKDF and secretstream symbols. The read-only build verifier passed the actual pinned source/build and rejected five modified-build cases; the prototype runner's five boundary checks and positive preflight passed using the shared verifier. Every installed input is inventoried/hashed and every archive object's ARM64 platform/minimum-OS is checked. The finance validation extension was moved byte-for-byte out of the mutable store file, without behavior changes.

Tests01 is retained: five methods passed and the isolation method had two unordered-JSON byte-comparison failures; file hashes already matched. Tests02 uses stable sorted-key comparison and passes. No runtime change was required for that correction.

Exact source, fixture, library, app and test binary hashes are in [v4-app-module.json](v4-app-module.json). Raw logs/results remain under `apps/ios/.build/v4-app-module/`; the F70 simulator was shut down afterward and the normal demo was untouched. Physical-device execution, packaging/signing, provider input, live v4 import/export, storage/candidate linkage and performance/capacity claims remain outside this proof. The local build report is provenance from the authenticated-source build, not a signed third-party binary. Approximate agent usage: 11,000 tokens, including the final focused correction/evidence.

The metadata-policy overlay subsequently passed **2/2 focused methods**: the real encrypted positive/license control and two valid 4,000-expense streams. The shared core accepted both, while the app rejected body metadata one byte above its current 15,727,872-byte plaintext cap and a body 128 bytes below that cap whose BEGIN/END overhead crossed it. Both early `nonReceiptBytes` and final `policyMetadataBytes` now enforce that cap. This is a conservative v4 read policy, not exact schema3 exportability. The prior six-method source/results and device-build scope are preserved separately in the JSON; the final simulator binary/source pins describe this overlay. No unrelated suite or device build was repeated for the two cap checks. Additional usage approximately 2,600 tokens.
