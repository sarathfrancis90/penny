Internal v4 preparation now feeds the existing guarded receipt candidate and local installation protocol. It captures the receiving target before decoding, copies at most 20 MiB of ciphertext into one private unlinked read-only inode, and runs two complete authenticated/native-validation passes on an operation-owned actor. Pass1 retains receipt-free models/descriptors; pass2 encrypts one borrowed validated receipt at a time. Both digest/count and complete summary/transcript must match. Source, pass readers and the snapshot close successfully before candidate finish; final local installation uses the existing stale/key/publication/rollback guards and Snapshot hydration.

Final candidate tests passed **7/7** on isolated F70/iPhone16e/iOS26.4.1. They prove exact all-domain/receipt install and fresh-store reopen, one-shot installation, three shared 1 MiB split-boundary installs, 10,001-record rejection, wrong-key/framing/tamper/native-image rejection, original-target edits, key loss with no provisioning, partial-read/close/cancellation cleanup, and isolation from replacement of the original input path. The input-copy probe confirms off-main execution. The private inode has no pathname or writable descriptor after capture; direct mutation of that inode was not injected.

The preceding combined run passed **29/29** (candidate6, existing preparation/install16, app codec7). Its app metadata-limit regression also proves provisional events are discarded when final BEGIN/END overhead exceeds the cap. Final7 adds split/cap installation coverage and checks the real parent-directory FD close before snapshot success. No full29 rerun or physical-device result is claimed. Cancellation and close failure coverage uses stated injection points, not a full-volume/physical-I/O failure experiment.

[Exact source, fixture and final executable hashes](v4-candidate.json) distinguish those checkpoints and map all ten shared acceptance IDs. Raw results remain in `apps/ios/.build/v4-candidate/`. F70 was shut down; the normal demo was untouched. There is no UI/provider dispatch, v4 export, raised capacity or performance claim. Approximate agent usage for this slice including final proof: 16,000 tokens.
