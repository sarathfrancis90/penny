# Files v4 export checkpoint

The ordinary app build passed **11/11, zero skips**: three coordinated Files destination tests, six native writer tests, existing legacy Files restore compatibility, and the unchanged real Files picker cancellation journey. Exact sources, binaries, fixture scope and results are pinned by `v4-files-export.json`. Prior writer19 proof and public native interchange fixtures remain unchanged.

Save encrypted backup now establishes a durable identity for a ready empty vault, captures an authenticated receipt-free source, and uses the native v4 writer. Each capture creates one fresh export snapshot UUID/timestamp; the vault identity, captured domains and receipt bytes are preserved and live snapshot metadata is unchanged. Confirmed recovery-key, action identity, cancellation and preparation revision guards remain on the UI boundary.

A worker holds security-scoped access to the selected folder. It coordinates exclusive creation of a new ciphertext file, copies bounded blocks from the opaque reader that enforces exact EOF/hash, then checks real sync and close. Separate coordinated native readback checks complete frame/logical/image semantics, expected ciphertext and summary before destination ownership is released. The selected parent permissions are not altered. New file creation retains strict device protection and the explicit simulator filesystem mode. File and directory identities are checked through held descriptors; failure cleanup cannot remove existing or replaced files. A late failure after release can leave an unconfirmed encrypted file and never claims provider upload completion.

The destination tests execute successful export and exact fresh-store restore, no-overwrite, replaced-inode preservation, wrong readback key, and cancellation/fault cleanup. Write/sync/close exceptions are injected after the actual filesystem call; they are not physical disk-full or hardware durability tests. Fresh-store reopen uses another instance in the same process. Successful remote upload, physical-device protection runtime and capacity/performance increases remain unproved.

A separate opt-in successful-picker diagnostic did **not pass**. Picker01 used the wrong Browse selector; its UI tree identified the real tab-bar control. Picker02 corrected only that selector and reached Browse → On My iPhone, where the system displayed **“On My iPhone is Empty”**. It did not expose the test app's precreated `Documents/PennyExportProof` folder. No success was inferred and no provider workarounds or further attempts were made. Both failed results and the corrected attempt's distinct app/Info.plist/UI-test hashes are preserved under `artifacts/offline/v4-files-export-ios/picker-01` and `picker-02`.

`FilesExportFlowTests` is opt-in with `PENNY_FILES_PICKER_PROOF`, because it requires the explicit test-only display name `Penny Files Proof` and ordinary setup of that folder in the installed `ca.penny.offline.v4filesexport` sandbox. It changes no app export behavior and does not run as an unconfigured normal CI test. The final project/test overlay is pinned separately from the passing11-method production checkpoint. The normal demo was untouched; F70 was verified shut down after testing.

Read-only follow-up inspected the actual installed app: `UIFileSharingEnabled` is absent, although `LSSupportsOpeningDocumentsInPlace` is true. The current Documents container retains only the empty `PennyExportProof` folder and no regular files. The prior ordinary writer build also lacks the file-sharing key despite its build setting. These facts are captured in `picker-02/installed-diagnosis.json`; the empty listing does **not** establish an external provider defect. No additional code or UI attempt was made for this diagnosis.

Configuration follow-up: `project.yml` now places both intended Files keys directly in `info.properties`, replacing their build-setting-only declarations. The generated, built and actually installed Info.plist each contains Boolean `true` for `UIFileSharingEnabled` and `LSSupportsOpeningDocumentsInPlace`. The isolated corrected build's separate `Picker03` proof and installed hashes are recorded in `picker-03/provenance.json`.

Picker03 selected the newly visible app folder, entered `PennyExportProof`, and tapped the real Open button. Neither of its two tests passed: the positive test then exceeded XCTest's 128-character identifier limit when querying the success message; the unchanged Cancel test expected a navigation-bar Button whereas the nested-folder Files presentation exposed Cancel as a host Other element. These are observed harness issues, not evidence of worker failure or successful completion. The bounded follow-up stopped without another UI attempt. Prior passing11 and failed diagnostics remain separate.

Final UI completion: the positive real Files journey **passed** in Picker04 after changing the long success query to an exact-label NSPredicate. It navigated to the test app's local folder, selected it with Open, and observed the app's full export-and-reopen confirmation; its screenshot and actual saved ciphertext are preserved. The Cancel method in that same run failed because its host accessibility overlay was not interactive. Video inspection showed the nested folder's visible Back/More/Open toolbar. The final test follows actual Back controls to the picker root before tapping visible Cancel, then asserts dismissal, cancellation text and readiness after relaunch. This method **passed1/1** in Cancel05. These separate proofs supersede the earlier unproved UI outcome without relabeling any failed run. Their exact test/app/Info.plist hashes are in `completed-ui-proof.json`; successful save was not rerun on the subsequent Cancel-only test overlay. Production export source is unchanged from the ordinary11 gate apart from the explicitly recorded Info.plist correction.

Filesystem capture after the passing UI run found its new 819-byte ciphertext at the test app's **Documents root**, not within the empty setup subfolder. The saved file `Penny-25149695-7f75-4f88-b565-be808cc5cb2c.pennybackup` has SHA-256 `676653165ba767194969ddd0e36ac3ad3a8acc2bec0eaf35f5c77107889eed1c`; exact current container path and the copied artifact are recorded in `picker-04/saved-destination.json`. The key is the public synthetic `01` test key. This is the actual selected local folder outcome; no provider upload claim is made.
