# Android P2 validation — 2026-09-13

Scope: native local expenses and receipt snapshots. This is not release approval or complete personal-finance parity.

Environment: JDK 17, Gradle 9.5, SDK/API 36, headless `BEK_Compact_Standard_API_36`, `emulator-5556`. UI tests use application ID `ca.penny.offline.dev.test`. No development/user vault was reset. Source edits remained inside `apps/android/`; root staging/docs are owned by the integrator.

## Completed gates

- `./gradlew -PpennyTestSandbox=true :app:connectedDebugAndroidTest :app:testDebugUnitTest :app:lintDebug`: success in 23 seconds. Connected XML reports 8 tests: 6 passed, 2 intentionally skipped because external process orchestration is required. JVM: 8 passed. Lint: 0 errors, 13 warnings (version/target updates, missing launcher icon, optional KTX style). Logs: `p2-complete-gates.log`, `p2-device-results.xml`, Gradle reports under `app/build/`.
- `sh verify-process-persistence.sh`: both separate instrumentation phases passed. UI-created CAD 34.56 expense survived external force-stop and cold launch; process 8306 was stopped and replaced by 8354. New process verified exact amount, then deleted only its fixture. Logs: `process-create.log`, `process-restart.log`, `process-verify.log`.
- Device tests: expense create/edit/activity recreation/delete; encrypted expense reopen and missing-key recovery; bundled OCR without Internet permission; receipt photo with no readable text retained through manual save/recreation/view/removal; iOS-runtime-produced v2 import; exact receipt bytes; injected SQLite abort preserves previous expense, receipt, vault ID; invalid image with intact PNG framing/CRC fails native decode; stale restore revision refuses to erase a newer expense; PNG above 1.6 MiB survives reopen and backup through bounded ciphertext reads.
- Final focused emulator rerun after adding the runtime artifact output: 2 passed in 5.594 seconds (`runtime-proof.log`), covering restore/failures and receipt UI.
- Actual emulator-runtime export: `interoperability/android-runtime-v2.pennybackup`, 1213 bytes, SHA-256 `3fc514f4eb124f96c5d67201ad0adf37d0f802f9dd620d83495110fc722a338f`. Uses the public shared fixture recovery key. The iOS source fixture is `interoperability/ios-produced-v2.pennybackup`. Neither contains user data. Earlier `app/build/android-backup-v2.pennybackup` is produced by the Kotlin JVM unit test, distinct from this Android runtime export.
- `receipt-viewer.png` is a device screenshot of the synthetic one-pixel fixture displayed in the receipt dialog; it demonstrates UI reachability, not receipt readability/design quality.

## Diagnosed failures and fixes

- The preceding 603-second Compose idle failure was not reproduced after restarting the absent emulator and using an isolated application ID. No unproven runtime fix is claimed. Repeated current UI journeys passed. Espresso idling is now bounded to 45 seconds; offscreen lazy editor actions use container `performScrollToNode` and UI selectors use unmerged semantics.
- BitmapFactory accepted truncated PNG bytes. Complete PNG chunk boundaries/CRC/IEND checks and API 28+ ImageDecoder rejection of partial decode were added. Tests also reject APNG and overlarge dimensions.
- A complete encrypted base64 receipt can exceed SQLite CursorWindow limits. Reads now retrieve ciphertext in 256 KiB slices; the large-image device test proves reopen and backup without lowering the shared 2 MiB cap.
- One retry was interrupted by an adb connection refusal. The emulator remained available and a later complete run passed; no data-clearing workaround was used.

## Remaining boundaries

No automatic Google Drive transport, remote upload verification, camera capture, provider-picker E2E, recovery-key re-entry ceremony, actual disk-full/process-death-during-commit test, physical-device hardware recovery, API 26–27 image decoder matrix, full accessibility/device matrix, or store signing/release is established. Snapshots still contain only expenses and receipts; budgets/income/savings/recurring/migration are a later schema. The previous Compose timeout remains an intermittent historical failure, not a proven code root cause.
