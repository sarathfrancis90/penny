# Independent backup review — 2026-09-13

Scope: final source review of both native expense/snapshot decoders, backup AES-GCM encoding, local key and restore paths, and external-file size bounds. This is a bounded implementation review, not a security certification or complete release gate.

Previous findings now fixed by source inspection: unknown/duplicate JSON members; Android coercive text decoding and malformed UTF-8; expense count/code-point limits; iOS bounded external reads and encoded export limit; canonical base64; separate confirmed restore paths for unreadable vaults; Android database under `noBackupFilesDir`. Both apps validate before persistence and preserve ordinary local edits' fail-closed behavior. Native owners report their own unit/UI results separately.

Independent test: both **native-produced** encrypted backups decrypt using the shared Node reference and public golden test key. Decrypted expense arrays, attachments, vault ID, and schema version match the golden snapshot exactly. Backup-created timestamps and snapshot IDs may differ.

| Native writer | File | Result |
| --- | --- | --- |
| iOS CryptoKit XCTest export | `apps/ios/.build/attachments/96677521-6F92-4970-A622-EC6DB5D601D4` | PASS; 799 bytes; one expense; 1,234 CAD cents. |
| Android/JVM native backup implementation export | `apps/android/app/build/android-backup-v1.pennybackup` | PASS; 781 bytes; one expense; 1,234 CAD cents. |

Reproduce from repository root while local build artifacts exist:

```sh
node --input-type=module <<'JS'
import { readFileSync } from 'node:fs';
import assert from 'node:assert/strict';
import { openBackup } from './packages/offline-contract/contract.mjs';
const fixture = JSON.parse(readFileSync('packages/offline-contract/fixtures/snapshot-v1.json'));
const key = JSON.parse(readFileSync('packages/offline-contract/fixtures/golden-vector.json')).recoveryKey;
for (const [platform, path] of [
  ['ios', 'apps/ios/.build/attachments/96677521-6F92-4970-A622-EC6DB5D601D4'],
  ['android', 'apps/android/app/build/android-backup-v1.pennybackup'],
]) {
  const bytes = readFileSync(path);
  const actual = openBackup(bytes, key);
  assert.deepEqual(actual.expenses, fixture.expenses);
  assert.deepEqual(actual.attachments, fixture.attachments);
  assert.equal(actual.vaultId, fixture.vaultId);
  assert.equal(actual.schemaVersion, fixture.schemaVersion);
  console.log(platform, 'PASS', bytes.length, 'bytes');
}
JS
```

Capacity item closed by current source inspection: both local save paths now enforce the common export predicate `4 * ceil(snapshotUTF8Bytes / 3) + 1024 <= 20 MiB`, in addition to the 15 MiB plaintext limit. JSON writers avoid escaped slashes. Both actual encrypted envelope writers still enforce final byte size.

Coverage boundary: these exports prove native writers interoperate with the shared reference. They do not prove each native app imported the other app's produced file through its document UI. Physical-device key loss, cloud authorization, receipt/full-domain backup, failed-write/process-death rollback, and verified cloud upload remain later release gates. Normal atomic-file/SQLite transaction code does not by itself establish crash recovery or retention of a verified rollback generation.

## P2 receipt contract and independent review

The shared reference now accepts strict snapshot versions 1 and 2, with v2 inline receipts under the unchanged authenticated envelope. `node --test packages/offline-contract/contract.test.mjs` passes 21 tests; `npx eslint packages/offline-contract/*.mjs` passes. Fixtures exercise complete receipt ciphertext/bytes/digest, v1 backward read and lossless native-schema upgrade, digest/media/size mismatch, missing owner, duplicate ID, unknown path/URL fields, unsupported schema, count/aggregate/export bounds, malformed headers, excessive image dimensions and lone UTF-16 surrogates. The public PNG is synthetic and contains no financial information.

Independent v2 native writer checks: Android `apps/android/app/build/android-backup-v2.pennybackup` and iOS `apps/ios/.build/p2-attachments/CB045DB8-5088-43D2-B47D-ECA302418653` both open with the Node reference and `golden-vector-v2.json` test key. Their expense and attachment arrays match `snapshot-v2.json` exactly, including all receipt bytes and SHA-256. Both results: PASS, 1,213 encoded bytes, one complete receipt. The Android artifact contains no escaped slash sequences. Importing the other app's produced archive in native tests remains a separate gate at this checkpoint.

Source review confirms iOS receipt replacement is part of the same encrypted snapshot write, with staged authentication/reread and a separate encrypted rollback file. Android stores receipt ciphertext under authenticated `receipt:<id>` context and uses SQLite transactions for expense-plus-receipt save, expense deletion and complete restore. Restore checks image decoding before replacement and rereads encrypted replacement rows before committing. Both platforms preserve existing expense receipts during unrelated expense edits. Unknown attachment keys cannot become filesystem paths.

Findings fixed by Android source inspection: direct merchant/note construction rejects lone surrogates; integer decoding accepts integral numeric spellings through exact decimal conversion; PNG preliminary validation enforces the first IHDR size; valid receipt retention survives OCR failure; a generation token prevents cancelled asynchronous scans populating a later draft. The shared contract now explicitly permits static JPEG/PNG only and rejects APNG `acTL` through a bounded PNG chunk walk, including single-frame animation containers. The invalid image fixture has a matching SHA-256; native adoption and final tests remain pending at this checkpoint.

New capacity concern sent to Android owner: a 2 MiB receipt becomes approximately 2.8 MiB of encrypted base64 JSON in one SQLite row. A device CursorWindow limit may prevent reading this row after a successful write. A valid image near the maximum must be saved and reopened on-device before closing the capacity gate; tiny fixture success does not establish that bound. If reproduced, encrypted chunk storage or bounded blob reads must preserve the shared 2 MiB per-receipt contract.

Independent migration source review found that combining both nonempty legacy `description` and `notes` into one labeled string loses the separate original fields. The integrator added a quarantine check (`distinct_note_fields_require_provenance`), verified by source inspection. Explicit profile/preferences exclusion means this converter cannot establish complete-account migration.

The reference checks image signatures/dimensions and PNG chunk boundaries, not a full PNG/JPEG decode. Native device tests remain necessary for image admission and failures. iOS now synchronizes staging, rollback and live files and locks normal writes if rollback replacement itself fails; these changes were inspected. Injected write exceptions demonstrate those exercised paths; they do not establish physical-device power-loss durability. Inline receipts remain bounded to 2 MiB each, 8 MiB total and 100 per vault; large-vault performance and a scalable storage format remain release gates.

## P4 finance review and runtime interoperability checkpoint

This checkpoint supersedes the earlier pending source findings above. The Android receipt reader now retrieves encrypted SQLite blobs in 256 KiB slices, avoiding a single CursorWindow row containing the full base64 receipt. Its native owner records a large-image reopen/export test in `apps/android/evidence/P2.md`. The legacy migration now targets schema 3, preserving distinct expense `description` and `note` fields; complete-account migration remains unestablished because excluded profile/preferences, unresolved source conflicts and unrepresented history must still be accounted for.

Independent read-only review covered both platforms' schema 3 models, stores, finance calculations and editors. Checked paths include aggregate money bounds, finance-only missing-key detection, strict v1/v2 upgrade, complete restore of every array, atomic and idempotent occurrence posting, references to edited or deactivated sources/templates, preservation of historical savings names and separate expense text, Gregorian boundary dates, and CSV formula handling after Unicode whitespace. The P4 source and local native regression gates are closed by the corrections and results below. This is bounded source inspection, not exhaustive verification of every screen or crash state.

The review found Android used Kotlin's default trim for merchant and finance names, allowing a leading U+FEFF that Node and iOS rejected. Current source shares the explicit ECMAScript whitespace predicate between name validation, UI normalization and CSV, and includes merchant/income-source rejection tests. The parent separately found iOS January 0001 rollover could compare two absent previous months and force unwrap; the owner corrected the boundary. Native owners retain responsibility for recording their final post-fix unit and UI results.

The iOS owner found Foundation `JSONSerialization` removes a leading U+FEFF inside JSON string values during the strict-decoder upcast/reserialization path, which can lose note content. The correction is independently source-verified: original bytes now reach Codable after strict shape checks, and Codable also preserves keys during inspection. Version-specific key and array guards still precede old-schema defaults. Native regressions cover v3 note/description content, an unknown FEFF-prefixed key and a legacy v2 note. The owner reports `apps/ios/.build/P4Accepted.xcresult`: 27/27 passed, zero failures/skips (24 unit and three UI), including the Unicode regressions and mandatory actual Android runtime v3 import. Android's owner reports 12 JVM tests and lint passing; the full device run discovered 12 tests with 10 passing and two intentional separate-process skips, whose two process phases separately passed with different process IDs.

The shared reference's 39 tests and targeted ESLint pass at this checkpoint. Tests cover snapshot versions 1–3, complete authenticated receipt/finance roundtrips, strict invalid-field corpus, deterministic Gregorian recurrence, bounded aggregate arithmetic, explicit received-income cash, savings ledger totals, contiguous positive budget rollover, idempotent posting, Unicode CSV prefixes and the separately staged conservative capture corpus. The P3 native capture handoff remains held; passing the shared parser tests does not establish native OCR/model behavior.

Both actual native runtime v3 exports independently decrypt and validate with the public `golden-vector-v3.json` key. Every expense, attachment, budget, income source/entry, savings goal/entry and recurring template matches `snapshot-v3.json` by ID, including receipt bytes and separate text fields. Android's expense array is exported in a different display order, so comparison sorts by ID. Snapshot ID and creation timestamp are runtime-generated.

| Native writer | Shared captured file | Encoded bytes | SHA-256 |
| --- | --- | --- | --- |
| Android emulator app runtime, SQLite reopen then Android AES/GCM export | `packages/offline-contract/fixtures/native-exports/android-v3.pennybackup` | 5,829 | `a5cd5622ed8bb4937aedf64433779d18384109f833437ce9c5fa8ec25bb5ded0` |
| iOS simulator XCTest app runtime, CryptoKit export | `packages/offline-contract/fixtures/native-exports/ios-v3.pennybackup` | 5,829 | `4d30be785b2865d377310ca31048444cda12a69015ac53555a0c29aa12626db8` |

The captured v2 and v3 archives, original runtime paths and public key references are recorded in `fixtures/native-exports/provenance.json`. The Node capture test requires all files, checks their hashes and compares every domain; the fixture generator does not recreate native captures. Both native owners received the shared v3 paths for mandatory cross-writer import checks without build-directory dependencies. Those native results and the final finance UI journeys remain separately recorded gates.

Remaining limits: simulator/emulator results do not establish physical-device key-loss recovery or power-loss durability. Receipt count/size limits remain the bounded inline baseline, not substantial-vault capacity acceptance. Scheduled income and recurring expenses remain reviewable proposals; savings withdrawals and cloud backup/sync are not implemented by this finance slice. Stored notification preferences do not prove notification authorization or delivery. Migration source completeness and complete-account retirement remain independent gates.

## P5 shared protocol checkpoint

The shared contract now includes a distinct AES-GCM cloud manifest context, immutable snapshot descriptors, opaque account/vault binding derivation and independent public crypto vectors. Portable snapshot bytes remain unchanged and restore needs no previous installation metadata. Native adapters have received the exact contract and fixture paths; no provider adapter or real-account result is established by this checkpoint.

The deterministic publication tests exercise all four provider suspension points: account change (including switching away and back via the binding epoch), cancellation, provider failure, bad snapshot size/digest/authentication, invalid manifest staging/readback, delayed callbacks, stale local revisions and interleaved writers. Only complete authenticated readback updates last-good state; successful publication of an older local revision leaves changes pending. Listing is bounded and incomplete/ambiguous results fail explicitly. Histories retain both writers and expose revision conflicts. No automatic deletion is emitted. These model tests use synthetic completion events and do not prove provider identity, upload completion or remote durability.

A root benchmark exposed a JavaScript stack overflow in the repeated-group base64 regex for a valid multi-megabyte archive. The production reference now uses a linear alphabet/padding scan plus canonical decode/reencode. A regression authenticates and opens a valid encoded archive larger than 19 MiB and rejects similarly long malformed base64 without stack overflow. Both native owners were notified to inspect equivalent regex paths. The shared package passes 49 tests and targeted ESLint at this checkpoint; native large-payload execution remains its own evidence.

External P5 gates remain real account consent, matching OAuth/signing registration, iCloud entitlements, provider upload completion, verified download and explicit clean-install restore. Automatic backup, retention/deletion and cloud availability must not be marked implemented from these vectors or model tests.

## P3 native capture review checkpoint

Read-only review inspected Swift/Kotlin receipt parser ports, optional model grounding, camera/OCR cancellation and failure handling, protected recovery-key storage, export staging and destination verification. Native owners' initial corpus runs passed the original 20 cases. Accepted image bytes are retained independently of OCR/model success; asynchronous generation/cancellation guards prevent completed work reopening a discarded draft. Model outputs pass source-grounded merchant/amount and canonical category checks before use; no action or cloud fallback path was found.

The corrections below are independently source-verified. The bounded P3 source and simulator/emulator regression gates are closed by the post-fix results recorded below:

- Android Unicode case-insensitive regex accepted Turkish dotted/dotless I as ASCII I in labels, allowing `Total Paıd 20.00` to become a CAD amount although the shared parser rejects it. The port now explicitly handles ASCII/French/simple-fold label matching while preserving original capture spans. Two differential cases expand the shared corpus to 22; reference parser tests pass.
- iOS protected recovery-key confirmation now reads back the exact stored key and restores the prior protected key on verification failure before returning an error.
- Both platforms use fresh UUID export names. iOS now selects a folder and coordinates an exclusive `withoutOverwriting` destination write, followed by exact authenticated readback. Android opens a nontruncating descriptor and requires known zero length before writing. Existing nonempty destinations are rejected. The iOS final suite includes a separate prior successful archive surviving a newer export's readback failure.
- iOS “Suggest category on device” previously reapplied merchant/amount before inference, overwriting manual corrections even on failure/cancellation. Its dedicated category-only draft state now updates only category and ignores cancelled/stale results. A regression preserves corrected 25.00 while source OCR says 20.00.

The iOS result summary at `apps/ios/.build/p3-accepted-summary.json` was read independently: `P3Accepted.xcresult` reports **36 passed, zero failed/skipped**, on iPhone 17 / iOS 26.4.1 Simulator. The owner records 31 unit and five UI tests, including all 22 parser cases, protected-key verification, exclusive export and preservation of corrected draft values. A final error-message wording change was compiled after this suite; it does not add behavioral coverage.

Android evidence at `apps/android/evidence/P3.md` records **18 device tests discovered: 16 passed and two intentional external-process skips; 15 JVM tests passed; lint zero errors and 17 warnings**. Its separate force-stop phases passed with distinct PIDs 18146 and 18191. The complete gate and the standalone camera journey both use ordinary Compose touch on the enabled shutter and actual CameraX capture of the emulator's synthetic scene. The earlier tap failure was resolved by awaiting the merged enabled Button, without a semantics-action bypass.

These results close the exercised implementation gates. Physical camera/OCR accuracy, hardware generative inference, real Files/SAF provider behavior, physical key invalidation, interruption during writes, and complete accessibility/device coverage remain open. They do not establish dedicated cloud upload or remote durability. The later privacy inventory also distinguishes on-device ML processing from documented Android SDK metrics; P3 local test success is not evidence of zero SDK telemetry in a network-capable release.

## Legacy converter review

Independent read-only review compared `migrate-legacy.mjs` and `migrate-finance.mjs` with the retained bootstrap serializer, Flutter expense/budget/income/savings models and web savings contribution/calculation contracts. Checked ownership, bounded cursor chains, duplicate records, strict cents and dates, distinct description/note fields, income recurrence and actual-cash separation, savings opening balance plus complete contributions, cache reconciliation, encrypted output, source/provenance preservation and exclusive owner-readable file publication. No additional represented-value loss was found after the correction below. Explicitly excluded profile/preferences and unsupported history remain migration gates.

The review reproduced a date corruption at the lower supported boundary: `0001-01-01T00:00:00.000Z` in America/Toronto formats into the preceding BCE year, but omitting Intl's era relabeled it as `0001-12-31` AD while returning ready. The root correction explicitly requests the Gregorian calendar and AD era, rejecting out-of-range local years. Source and expense/income/savings regressions are verified; all 15 migration tests pass independently.

The subsequent null-type correction is independently source-verified: the converter accepts absent/null `expenseType` as personal, matching the retained Flutter model, only when group indicators are absent; explicit unsupported types and group markers still prevent replacement. All 16 migration tests now pass independently. This corrects conversion of a supplied legitimate record, not export coverage.

Source completeness remains separate from converter success. The bootstrap filters expenses to explicit `expenseType == personal`, whereas the retained Flutter model treats a missing/null expenseType as personal. Older documents without that field are absent from the exported cursor chain and require authenticated reconciliation before complete migration claims. Page watermarks are not a frozen source transaction, and complete savings history is an exporter assertion rather than proof. A ready result establishes conversion of represented file records only; it cannot establish complete authenticated account export or a production migration.

## P6 release/privacy and artifact-preflight review

[PRIVACY.md](PRIVACY.md) inventories the current native core and implemented optional provider transfers, with conditional store-answer drafts. Read-only source review distinguishes locally processed receipt/model content from documented ML Kit operational metrics; no observed SDK traffic is claimed. The final network-capable Android artifact needs SDK/AICore traffic evidence and accurate disclosures. iOS needs a final signed archive privacy manifest/report and required-reason API inventory. Existing retained releases, original receipt metadata, readable CSV and remote retention prevent blanket native-only privacy claims for the whole product. [RELEASE.md](RELEASE.md) records the machine, physical, account, migration and store gates.

Read-only review of `release-preflight.py` initially found three false-acceptance paths. Valid-fixture mutations accepted a Production app with a Development-only CloudKit profile and a profile containing no authorized signing certificate. A third case was reproduced by compiling a synthetic Android manifest with SDK 36 `aapt2`: true-valued boolean resources for `debuggable`/`testOnly` become `@ref/...` in `apkanalyzer manifest print`; comparing only to literal `true` incorrectly marked both false.

All three corrections are independently source-verified and the **12 post-fix policy/parser groups pass**. iOS signature verification now requires the expected certificate leaf organizational unit under Apple's trust anchor, extracts the actual leaf DER into an automatically cleaned temporary directory and compares its SHA-256 with the profile's authorized certificates. The profile must authorize Production as an exact value or array member. Android accepts only absent or literal-false debug/test flags; unresolved resources fail closed. The fresh 20:43 UTC reports for both actual development artifacts were inspected and record rejection. These close the three bounded findings, not Apple profile acceptance, a positive signed production-artifact gate or real provider operation. No scripts or native files were edited by this review. The subsequent bounded P5 native closure is recorded below.

## P5 native manual-backup review closure

Read-only source review covered both native manifest codecs, publication controllers, provider transports, persistent state and restore confirmation. Distinct AES-GCM AAD and opaque tags, exact schema/byte bounds, original portable snapshot version checks, full receipt validation, guarded provider effects and explicit all-domain replacement match the shared contract. Native CloudKit uses immutable record IDs with conditional save and explicit private-database fetch; Drive creates a new resumable upload session and sends ciphertext only to its validated fixed-host session. Existing objects are not overwritten or deleted. The final Drive POST/session/PUT flow was checked against [Google's upload protocol](https://developers.google.com/workspace/drive/api/guides/manage-uploads); fake HTTP tests establish request construction and rejection paths, not server acceptance.

The review and root integration found and closed these bounded implementation defects by source inspection and the native results below:

- Publication now checks a complete inventory before reserving/uploading: at most 99 existing recognized manifests and 998 total objects, including unknown objects, old-key manifests and orphans. Incomplete or ambiguous listings fail. This preserves room for the next two objects within discovery bounds; simultaneous independent writers can still race across capacity.
- Every attempted publication reserves a durable writer revision before remote effects, so retries after uncertain completion do not reuse a revision. Android records the backed-up data revision separately. Concurrent local edits remain pending.
- Android cloud restore retains account/session/key checks through the confirmation action and performs the guarded atomic replacement with its expected local revision. The earlier generic preview path lost those checks. Portable-file restore remains independent of provider access.
- Android persistent cloud state now binds the confirmed recovery key and hides/disables stale bindings after rotation. Continuation guards check protected recovery state as well as account, session, vault incarnation and bounded credential lifetime. Discovery excludes individual unauthenticated/invalid manifests with a visible count, allowing different-key generations to coexist untouched.
- Android post-write settings verification restores the exact prior ciphertext on failure; an incomplete-write marker fails closed after interruption. Native rollback/reopen and missing-key tests cover the exercised paths. iOS similarly restores and verifies its prior encrypted status after failed readback. These are not physical power-loss proofs.

Final evidence was read directly rather than inferred from Gradle progress counters:

| Platform gate | Evidence and result |
| --- | --- |
| iOS complete post-capacity regression | `apps/ios/.build/p5-accepted-final-summary.json`, `P5AcceptedFinal.xcresult`: **47 passed, zero failed/skipped** (41 unit, six UI), iPhone 17 / iOS 26.4.1 Simulator. |
| iOS conditional device code | Unsigned Release iPhoneOS compile with `PENNY_SIGNED_CLOUDKIT` succeeded. This proves compilation of the conditional branch, not entitlements, installation or CloudKit access. |
| Android complete API 36 regression | `apps/android/evidence/p5-device-results.xml`: **22 discovered, 19 passed, zero failures/errors, three intentional skips** (two external process phases and one opt-in network probe). |
| Android final targeted runtime | `p5-final-targeted-runtime.log`: **4/4 passed**, exercising protected state, native controller with fake transport, default-unconfigured UI and the explicit platform TLS probe. |
| Android final expiry regression | `p5-expiry-runtime.log`: **2/2 passed**. The controller test advances an injected monotonic clock by 600001 ms while upload is suspended and verifies the previous last-good state survives. |
| Android JVM and lint | Five current test XML files total **22 tests, zero failures/errors/skips**. Final lint XML has **zero errors and 18 warnings**. |
| Android external process phases | Separate create/restart/verify evidence records successful force-stop recovery across **PIDs 20093 → 20141**. These phases are not hidden failures in the full suite. |

The parent independently opened the actual Android controller-produced cloud manifest/snapshot pair and compared all eight arrays with `snapshot-v3.json`. The inspected report `artifacts/offline/android-cloud-independent-verification.json` records complete equality, fresh native snapshot metadata, manifest SHA-256 `7d4af63cd70b9f0f2d4b967447ca61ff358fe8638efbc61ee66c6cbbda4c6362` and snapshot SHA-256 `279b595133093cbef9bcccc31e649f107e7470e37ee5ecdf12a4ce848d5c50d7`. The files under Android's `evidence/interoperability/` contain public synthetic fixtures. Their transport was fake; the report explicitly records `remoteProviderVerified: false`.

These close the bounded **manual P5 source and local native regression gates**. Real signed OAuth/CloudKit entitlement/account behavior, server durability, clean-device provider recovery, physical device/key loss and SDK/AICore traffic remain open. The credential-free Android TLS probe establishes only the tested platform connections, not universal absence of telemetry. CloudKit owns CKAsset allocation before the app's bounded read. Concurrent-writer capacity and retention need further design; no automatic remote deletion is enabled. The subsequent sections record the separate P5b scheduling and API 37/16 KiB runtime results; manual backup tests alone did not establish those later gates.

## Native cloud captures and P5b review closure

The shared package now retains both platforms' original runtime cloud manifest/snapshot bytes under `fixtures/native-exports/{ios,android}-cloud-v1.*`. `cloud-provenance.json` records the source runtime, exact byte counts/digests, shared public synthetic key and fake Drive identity. Independent Node authentication verifies each manifest's referenced snapshot and compares all eight record arrays by ID with `snapshot-v3.json`. The iOS manifest/snapshot digests are `62a43ba98c6dc79baa00e62cd07075fb61b502aebfedaceed446badc8f1f696e` and `0905a7b61d85348f9503467b996bfa342df727ff446cc8696d5e568d4f1cc5f0`; Android digests match those above. The cloud suite passes **10/10** with ESLint and patch-whitespace checks. These immutable captures remove local build-directory dependencies from future cross-reader checks. Both provider transports were fake; they establish native cryptography and record preservation, not remote service durability.

P5b read-only review found that a failed automatic-settings write could roll back to a persisted enabled state and revive opted-out or exhausted work after restart. Both corrections persist independent stop markers before automatic-state mutations. Failed writes leave automatic work stopped until explicit successful re-enablement; permitted retries clear the marker only after their verified settings write. Android error handling marks stopped before attempting protected-state reads, and unreadable settings cancel scheduled work and clear the automatic-enabled UI. Reopen regressions cover failed opt-out, retry/attention updates and protected-state read failure. The source corrections are independently verified.

The review also found that Android's new worker uses a second `VaultStore` instance while `cloudCheckpoint()` ended its snapshot transaction before reading revision/incarnation. A foreground commit in that gap could label old records with a newer revision and suppress the next needed backup. The corrected checkpoint reads all components in one SQLite transaction. Its two-connection regression holds a foreground writer at the former gap and verifies the old snapshot carries the old revision, with the subsequent edit still pending. Network work remains outside vault and coordinator locks; local financial operations do not wait on remote I/O.

One iOS app-owned coordinator/publication instance and Android's process-wide lease serialize foreground actions, background callbacks and guarded restore. Account/key/restore changes, expiry and cancellation invalidate late continuations. iOS completion is guarded once and its encrypted retry count limits transient retries; Android uses a bounded coroutine timeout and WorkManager attempts, with explicit unique-work cancellation on required-action outcomes. `Result.failure()` alone does not terminate recurring work, so the persisted stop state and cancellation are essential. [WorkManager periodic lifecycle](https://developer.android.com/reference/androidx/work/PeriodicWorkRequest).

Final results were read directly:

| Gate | Result |
| --- | --- |
| iOS `P5bAccepted.xcresult`, `p5b-accepted-summary.json` | **55/55 passed, zero failed/skipped**: 48 unit and seven UI on iPhone 17 / iOS 26.4.1 Simulator. Includes failed-settings reopen, mandatory Android cloud capture restore, synthetic consent/relaunch and default-unavailable local operation. |
| Android `p5b-device-results.xml` | **26 discovered, 23 passed, zero failures/errors, three intentional skips**: separate process phases and opt-in TLS probe. API 36 emulator; Gradle progress counters are not authoritative counts. |
| Android exact-source `p5b-closed-runtime.log` | **8/8 passed**, including the final protected-read marker ordering, two-connection checkpoint, WorkManager/runner, mandatory iOS cloud capture, default configuration UI and explicit TLS probe. |
| Android JVM/lint | **24/24 passed**, zero failures/errors/skips; lint **zero errors, 18 warnings**. |
| Android external process phases | Create/verify each **1/1 passed**, with `process-restart.log` recording **PIDs 22761 → 22810** and a cold fresh-process recovery. |

These close bounded P5b source and local native regression gates, not release eligibility. Automatic backup remains default off and unavailable in unconfigured builds. Fake scheduler/provider tests do not establish actual OS wake timing, locked-device key access, signed consent, server durability or clean-device remote recovery. Simultaneous failure of stop-marker and settings storage and physical power loss require device evidence. Existing capacity limits and concurrent-writer races remain; no remote deletion is implemented.

## P6 local packaging review

Read-only review of `package-native.py` and its tests found two bounded correctness defects. The initial Android configuration used one fingerprint for both the locally signed upload artifact and Drive's runtime installed-app guard, preventing a working configuration when Play App Signing uses a different certificate. The correction requires separate `uploadCertificateSha256` and `driveSigningSha256`: packaging verifies the former against the APK and embeds/checks the latter against the independently registered installed-app identity. A differing-certificate regression confirms the two checks cannot substitute for each other.

The initial build report also repeated the requested build without comparing it with the observed preflight build. A different, newer artifact could pass the minimum-version gate while the report named the wrong build. Packaging now parses successful preflight metadata and requires the requested platform, exact version and exact build; it retains the observed report. The regression rejects requested build 10015 with observed build 10016.

Both corrections are independently source-verified; **17 packaging/preflight Python tests pass**. Packaging uses a new owner-only output directory and a hash-checked isolated source copy, rejects symbolic source links, checks the live source inventory for changes, and does not clean the developer's native build directory. Android signing passwords remain environment inputs and build output goes to protected logs. iOS uses explicit local manual-signing/export configuration without an upload destination or automatic provisioning flag. No signing, production packaging, provisioning or upload was performed by this review. The report explicitly leaves `storeUploadArtifactValidated` false: archive `.app`/APK checks do not establish the exact exported IPA/AAB, store-delivered signing, actual provider recovery or release eligibility. Native guarded build configuration and positive signed-artifact tests remain separate P6 gates.

## P6 parser and storage optimization review

The focused Swift parser review is closed. Keys-only snapshot inspection still requires exact root, record and schedule members, then decodes every original value into the full typed model and applies all validation and export-capacity checks. The string fast path uses failing UTF-8 decoding, preserves raw U+FEFF and rejects unescaped controls; escaped strings retain strict Foundation decoding. Duplicate-key and nesting checks remain. Cold loads and external restores still use the full strict decoder. Local commit skips an extra decode only after validating the typed state, encoding an immutable frame, authenticating staged/live readback and requiring exact equality with that frame.

`P6Worker.xcresult` / `p6-worker.log` records **49 unit tests passed, zero failures** after the change. The added raw-string regression checks distinct U+FEFF keys/values, malformed UTF-8 (overlong, encoded surrogate, truncated and invalid bytes), controls, escaped duplicate aliases, depth 33 and unknown schedule members. Existing shared malformed/golden and mandatory native cloud-capture tests remain green. The subsequent worker and Android storage reviews are recorded below.

## P6 prepared writes, wrapped row keys and final evidence

Read-only review of iOS `PreparedVaultWrite`, `ArchiveWorker`, `VaultStore.apply`, finance callers and cloud restore found no remaining storage-boundary defect. The worker fully validates the typed proposal, capacity and receipt content before encoding and sealing an immutable frame. Final replacement is non-suspending and checks the originating revision, writer and restore epoch. Staging/live authentication must produce exactly the validated frame; external import and cold open still require strict decoding. Cloud restore rechecks account/session/key state after preparation and immediately before applying. Regressions cover canceled/stale proposals, changed restore epochs, rollback and idempotent recurring posting.

The review found one iOS UI coordination defect: a canceled restore's late completion could clear the busy state of a newer export. All local archive actions now use `VaultActionState`; restore captures a fresh ID, and only the matching action may report success/error or finish. The shared state regression covers canceled restore → new export → late completion, and cancellation after picker resume. Source and focused results verify closure.

Android's random software row key is wrapped by Keystore; version, generation, wrapped key and every legacy record migrate in one SQLite transaction. All eight domains authenticate and validate before commit, including receipts read in bounded chunks. Confirmed full restore installs a fresh generation within the replacement transaction. The linear ASCII number scanner preserves strict RFC8259 token/structural rejection. No portable schema or key derivation changed.

Two Android findings are closed in source and runtime tests. Receipt deletion now authenticates the current key within its deletion transaction, preserving ciphertext/metadata if a previously selected receipt outlives missing/corrupt DEK or KEK state. The intermediate DEK cache was removed: older Android Keystore equality compares alias/UID/algorithm and cannot prove that a same-alias key has not been replaced. Every key operation now authenticates the current wrapper; row batches reuse only that operation's software key. The modern-emulator key-replacement test alone would not establish older-runtime equality semantics. [Android 8 primary implementation](https://raw.githubusercontent.com/aosp-mirror/platform_frameworks_base/android-8.0.0_r1/keystore/java/android/security/keystore/AndroidKeyStoreKey.java).

The following evidence was read directly:

| Gate | Result and scope |
| --- | --- |
| iOS full functional checkpoint | `P6Final.xcresult`, `p6-final-summary.json` and log: **60/60 passed**, zero failures/skips, **52 unit + eight UI**, iPhone 17 / iOS 26.4.1 Simulator. This binary compiled immediately before the final UI action-state correction. |
| iOS post-correction checkpoint | `P6LateCompletion.xcresult`, `p6-late-completion-summary.json` and log: **5/5 passed**, zero failures/skips, four worker unit tests plus recovery/export UI. Combined coverage contains 61 distinct normal tests; it is not a new 61-test full run. |
| iOS source/display freeze | All **51 current source hashes** in `p6-source-freeze.json` match the reviewed tree, including the later PNG validator. Small-phone and tablet visual summaries each record **1/1 passed**, with no failures/skips, before the PNG-only admission correction. These are functional rendering journeys, not a physical VoiceOver review. |
| Android final regression | Final `p6-final-results.xml`: **34 discovered, 31 passed, zero failures/errors, three explicit external-process skips**, Android 17 / API 37 / 16 KiB, **108.244 seconds**. All four row-key methods and the shared PNG method pass. The full run enables the performance and public TLS probes. |
| Android JVM/lint | Current test XML totals **25/25 passed**, zero failures/errors/skips; lint XML has **zero errors and 11 warnings**. |
| iOS privacy/platform inventory | `p6-privacy-build-audit.json` records the icon, four iPad orientations, bundled file-metadata reasons C617.1/3B52.1, direct `_fstat` use and absent debug-harness strings in an unsigned generic iPhoneOS Release compile. This is not final signed archive or App Store acceptance. |
| iOS accessibility diagnostic | `p6-accessibility-diagnostic-summary.json` records **one failed unfiltered audit**. The retained finding list contains **13 issues: ten Dynamic Type, two clipping and one contrast**, including app-owned labels. Functional/display passes do not resolve them; no category is disabled to manufacture a pass. |

The inspected post-PNG iOS performance JSON at `png-performance-final/711F606F-B099-44A3-B738-CC2293228117.json` records Release-optimized simulator 10k cold open **169.93 ms**, save **91.80 ms**, and save/restore-commit maximum sampled MainActor gaps **9.26/9.63 ms**. The unchanged 8 MiB/64 MP **JPEG** receipt matrix reaches **35.21 ms** during final commit; it is not a PNG-specific scale measurement. Heavy preparation is off MainActor; cold initialization and final disk replacement still have synchronous work. Android's final post-compatibility debug-emulator JSON records 10k reopen **1,108 ms**, save **1,160 ms** plus refresh **1,066 ms**, and portable decode **1,068 ms**. Maximum sampled PSS across that complete UI/OCR/scale process is **303,530 KiB**, not a true peak. Workloads and tools differ, so these numbers are not a platform comparison or physical-device guarantee. They do not increase the existing receipt/record/byte bounds.

Native source review is closed for these bounded changes. Local test evidence does not close physical power loss/disk-full during migration, hardware key invalidation, actual background wake/key availability, provider credentials/server durability, SDK/AICore egress, signed legacy upgrade, accessibility or store-installation gates. The unfiltered iOS audit remains open and must receive element-specific review. [RELEASE.md](RELEASE.md) and [PRIVACY.md](PRIVACY.md) retain these boundaries.

### Minimum-Android and shared PNG admission follow-up

Subsequent API 26 execution reopened a narrow compatibility slice: `SQLiteOpenHelper` did not supply the newer closeable interface assumed by Kotlin `.use`, and the old bitmap decoder returned partial pixels for damaged PNG input. Explicit `java.io.Closeable` fixes resource closing. The large legacy-record decrypt now uses 16 KiB updates, bounds accumulated output to ciphertext length minus nonce/tag overhead, requires final AES-GCM authentication and verifies exact plaintext length before returning. The original large-call runtime dropped output; no authentication or corruption fallback was introduced. Source review of these corrections is closed; the earlier runtime acceptance above does not establish their later tests.

The shared `png-integrity-corpus.json` first reproduced ImageIO admitting all seven malformed scanline/zlib inputs, while three valid controls preserved bytes. Its final frozen corpus contains **24 cases: five valid and 19 invalid**, covering those unchanged inputs plus the concrete aligned chunk-name/reserved-bit, critical-chunk, palette and animation rules. SHA-256 is `e2728df1e7c45b119ca5a70d288de1c3bf7280f2b98c3ee6b7a8476fa17b7e5c`; its Node test, independent checks of **473 chunk CRCs** and ESLint pass. Both native ports now check complete zlib data, exact scanline lengths, valid row filters and Adam7 passes before image decoding or conversion. Source review verifies bounded arithmetic/output, progress/termination, exact compressed-input consumption and matching container rules. [PNG filtering/compression specification](https://www.w3.org/TR/png-3/#9Filters).

The inspected iOS correction summary `png-integrity-accepted-summary.json` records **58/58 passed** (all 55 unit tests and three affected receipt/capture UI journeys), zero failures/skips. The final expanded corpus method then passed **1/1** in `png-corpus-final-summary.json`; both direct attachment admission and preparation preserve the five accepted originals and reject all 19 invalid inputs. The original ImageIO failure remains in `PNGIntegrityAdmission.xcresult`. This closes the narrow iOS source/runtime correction; unchanged UI evidence remains in the earlier full P6 result, rather than claiming a new complete 63-test run.

Android's inspected `p6-api26-final-results.xml` records **34 discovered, 29 passed, zero failures/errors, five intentional skips** on API 26. Skips are the TLS opt-in, scale opt-in and three separately driven process phases. The final expanded corpus passes **1/1** in `p6-api26-png24-results.xml`, including rejection before preparation and atomic replacement. Final API 37 results are recorded above; its test APK contains all **24 cases** with the exact frozen corpus SHA-256. All **45 current Android production-source hashes** match `p6-source-sha256.txt`. The final force-stop log verifies a UI-created expense across **PIDs 4651 → 4701**. The inspected `p6-process-open.json` verifies 10k records across **PIDs 4847 → 4909**, with **1,563 ms** fresh-process open and **145,671 KiB** sampled PSS; the OS disk cache was not flushed. These close the bounded minimum-runtime, PNG admission and final storage regression gates. Hosted CI and the physical/provider/accessibility/release gates remain separate.

## P6 clean integration and hosted gate scope

Independent inspection of the parent's isolated review-tree logs records **150 API tests**, **226 global tests**, **104 generated-doc tests**, **67 offline Node tests** and **19 offline Python tests** passing. Web typecheck/build logs are clean; the build used synthetic public Firebase configuration and an explicit Admin build fallback, so it is not an authenticated backend test. The audit JSON reports zero known vulnerabilities at this checkpoint. Final synchronization and docs regeneration remain the parent's integration responsibility; these logs are not evidence for unreviewed later changes or the user's excluded original API/Flutter work.

The native CI source now runs on every PR/push to `main` without path filters. Its always-running `native-offline-gate` requires success from contract, Android build, iOS and the Android device matrix; the repository's Penny Required Gate requires that aggregate check. The configured Android matrix includes minimum API 26 and API 37/16 KiB. Hosted execution is still pending; locally passing policy checks and a configured matrix cannot establish a completed hosted run.
