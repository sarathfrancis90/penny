# Penny Offline execution status

Updated 2026-09-13. See [PLAN.md](PLAN.md) for requirements and release gates.

## Release disposition

**In development. Not a release candidate.** The native work is isolated from the existing Flutter release and API. A passed build is not store readiness, backup success, or physical-device AI verification.

## Current work

| Workstream | State | Evidence / next gate |
| --- | --- | --- |
| Existing source baseline | Inspected | Current repositories use HTTP; encrypted shadow copy is not authoritative |
| Existing Flutter analysis | Passed | `flutter analyze`: no issues, 7.6 seconds on Flutter 3.41.3 |
| Existing Flutter simulator launch | Passed | iPhone 16 Pro Max / iOS 18.5; 295.2-second build; actual onboarding and guest UI inspected; local API override |
| Existing expense and local-store tests | Passed | Eight targeted repository/local unit tests, including cipher-at-rest test |
| Native iOS foundation, finance and capture | Passed locally | SwiftUI iOS 26; 36/36 P3 tests (31 unit, 5 XCUITest), zero skips, iPhone 17 / iOS 26.4.1 |
| Native Android foundation, finance and capture | Passed locally | Compose/Material 3; 15 JVM tests, 16 full-suite device tests plus two separately exercised process phases, lint zero errors and 17 warnings |
| Receipt storage and recovery | Local slice passed | Retained encrypted receipts, digest/ownership/image checks, staged iOS rollback and Android transactions; Android 6 device + 2 separate process phases + 8 JVM tests passed; both native v2 writers independently decrypted with exact receipt bytes |
| Portable contract | v1/v2/v3 and cloud-v1 reference passed locally | Shared Node fixtures passed; actual iOS/Android runtime exports independently decrypted and every record matched by ID; native opposite-platform v3/cloud imports mandatory |
| Legacy migration preflight | v3 slice passed locally | Sixteen converter/CLI tests; exact cents, domain settings, separate notes, savings reconciliation, calendar-era bounds, legacy null-type default and protected no-overwrite output; independent review found no remaining represented-value loss |
| Native CI | Implemented; hosted run pending | Contract, Android build/device/process and iOS simulator jobs; the aggregate native gate is now included in Penny Required Gate, and runs on every main PR/push so path skips cannot bypass it. Hosted execution and branch-protection settings remain unverified |
| Finance completeness | P4 local slice passed | Budgets, received income, savings, recurring review and reports/CSV; both UI journeys verify CAD 800 income, 12 expenses, 788 net and 50 savings allocations after relaunch |
| Receipt capture and recovery ceremony | Both local P3 gates passed | Matching 22-case OCR parser, grounded on-device model proposals, memory-only camera/image preparation, full recovery-key re-entry and readback, immutable verified export; Android ordinary Compose touch exercises actual CameraX capture through exact receipt backup |
| Cloud backup | P5/P5b local gates passed | iOS 55/55 plus 2/2 final-copy UI checks; Android 24 JVM, 23 full-suite device passes, 8 targeted passes and separate process recovery; real accounts remain open |
| Major release/migration | Planned | Full domain parity, migration, physical devices, signed store artifacts and rollout remain open |
| Signed artifact preflight | Negative gates passed locally | Nineteen Python gate groups, including artifact policy, packaging and AVD metadata; actual development artifacts correctly rejected; Apple signing/profile/CloudKit and Android signing/configuration checks do not replace store validation |

## Baseline preservation

Before edits, the working tree already contained modified API tests/routes/services, Firestore indexes, generated OpenAPI/agent docs, Flutter dependency/project/provider files, and untracked local database/tests. The first baseline audit confirmed those application files were unchanged. A subsequent durable hash baseline for 18 legacy files is stored in ignored `artifacts/offline/legacy-baseline-hashes.json`. Initial temporary logs/screenshots are no longer available after continuation; their observations above are historical execution results, not durable artifacts. Existing generated files may be regenerated by the mandated docs command; pre-existing application changes must otherwise remain intact.

## Evidence and coverage boundaries

- Root contract, migration, lint and CI policy logs: ignored `artifacts/offline/`. Shared contract and migration suites now total 67 tests, plus nineteen Python gate groups and static category/dependency/network-policy checks. Static checks do not establish runtime network behavior.
- Android instrumentation/process logs: ignored `apps/android/evidence/`; native reports under `apps/android/app/build/`. The process test creates a marked expense in the isolated `.dev.test` app, force-stops its PID, starts a different process, verifies the expense and removes only that fixture. An earlier 603-second Compose idle timeout did not reproduce on the isolated test app; its root cause is not claimed fixed.
- iOS XCTest results: ignored `apps/ios/.build/`, including P2 baseline/final result bundles. Signed simulator tests exercise device-only Keychain access; simulator success does not prove hardware security or real supported-device Foundation Models behavior.
- Accepted P2 evidence: `apps/ios/.build/P2Accepted.xcresult`, `apps/ios/.build/p2-accepted.log`, and `apps/android/evidence/P2.md`. Android's first portable writer fixture was produced on the JVM; a separate actual emulator-runtime archive was subsequently exported and independently verified under `apps/android/evidence/interoperability/`. Injected write failures and normal relaunch do not establish physical power-loss or full-disk durability.
- Accepted P4 evidence: `apps/ios/.build/P4Accepted.xcresult`, `p4-accepted.log`, `p4-accepted-summary.json`, and `apps/android/evidence/P4.md`. Tests include earliest supported budget month, month-end scheduling, formula-safe Unicode CSV, FEFF preservation through legacy and current decoding, and actual opposite-platform runtime backup import. Android's full suite intentionally skips two tests that require separate instrumentation processes; `verify-process-persistence.sh` ran both phases successfully under different PIDs.
- Accepted iOS P3 evidence: `apps/ios/.build/P3Accepted.xcresult`, `p3-accepted.log` and `p3-accepted-summary.json`: 36 tests, no failures/skips. The focused gate includes all 22 shared receipt cases, proposal rejection/cancellation, preserved manual corrections, HEIC/image preparation, Keychain confirmation readback/rollback, exclusive export collisions and failure preservation. Real UI exercises camera-unavailable manual entry, cancelled drafts, full recovery-key re-entry/mismatch/relaunch and folder-picker cancellation. Actual physical camera capture, hardware file-protection enforcement and model generation remain unverified.
- Accepted Android P3 evidence: `apps/android/evidence/P3.md`, `p3-final-verified-gates.log` and `p3-device-results.xml`: 16 device tests passed; two process phases intentionally skipped in that suite then passed under distinct PIDs 18146 and 18191. All 15 JVM tests passed. The actual emulator camera journey captured, prepared, reviewed, saved and recovered the exact encrypted receipt. Earlier camera test failure came from observing the Text child's enabled state before its parent shutter Button was ready; awaiting the merged enabled Button fixed the test with ordinary Compose touch. Physical camera and Nano inference remain unverified.
- Physical-device inventory currently lists an unavailable iPhone 13 Pro Max. No attached supported device has established Foundation Models or Nano behavior. Simulator/runtime verification continues independently.
- The current backup UI exports an encrypted document through the OS file picker. iCloud Files/Drive providers are not proof of automatic cloud backup, provider account isolation, remote upload completion or clean-device recovery. Dedicated transports and scheduling are implemented and fake-provider tested; real-account recovery remains open.
- Android P5 adds Internet access for the explicit Drive adapter, with platform TLS default-deny and system trust restricted to the exact Drive host. ML Kit documents operational metadata collection despite on-device inference. That policy does not establish custom SDK/AICore traffic behavior; final binary traffic observation and accurate store disclosures remain required. See [PRIVACY.md](PRIVACY.md).
- Accepted manual P5 evidence: iOS `P5AcceptedFinal.xcresult` and `p5-accepted-final-summary.json` contain 47 passes (41 unit, 6 UI), no skips/failures; unsigned Release iPhoneOS compilation also exercises the real-device cloud compile condition. Android `evidence/P5.md` records 22 JVM passes, 19 full-suite device passes with three explicit skips, separately passed process phases, four targeted cloud/UI/TLS passes and two fake-clock expiry passes. Android runtime manifest/snapshot bytes were independently decrypted and all eight fixture arrays matched. Real provider publication/recovery and background operation are not established by these fake transports.
- The existing project's Drive API was enabled and independently read back on 2026-09-13; no account consent or user backup transfer occurred. Existing production OAuth registration has a matching local development signer; isolated native development packages remain unregistered. iCloud container/profile setup remains outstanding. See [PROVIDER_SETUP.md](PROVIDER_SETUP.md).
- Android now compiles/targets API 37. The separate Android 17 emulator is running with verified 16 KiB pages; final P6 regression/performance/display evidence is recorded in the platform README/evidence. CI now targets the minimum API 26 and current API 37/16 KiB with runtime assertions; hosted execution remains pending. Older avdmanager generated target=android-0 for the 37.0 image, disabling acceleration; the CI prelaunch guard verifies the installed image before correcting that specific metadata bug.
- Receipts currently accept static JPEG/PNG with limits of 2 MiB each, 8 MiB total and 100 per vault. The shared image reference checks structural bounds; native decoders additionally reject damaged images. These limits are a development constraint requiring measured redesign before large-dataset release.
- The retained JavaScript audit was reduced from 46 findings to zero through forward dependency updates and tested Browserslist/esbuild overrides. Both expired exceptions and the obsolete scanner-config flag were removed. API typecheck/build plus 152 tests, web typecheck/production build, all 228 repository tests and the 20-workflow CI policy passed locally. Hosted CI, OSV/Trivy and production deployment remain unverified; see [COMPATIBILITY_SECURITY.md](COMPATIBILITY_SECURITY.md).
- Store APIs were inspected without publication: iOS listing 2.3.5 ready for sale; Android internal 2.3.5 / 10014, with production/beta returning no releases. The current iOS bundle lacks iCloud capability. See [RELEASE.md](RELEASE.md) for the exact scope and signing gates.

## Token accounting

The plan allocates a soft 120,000-token envelope across eight phases. Initial agent allocations are checkpoints, not enforced limits. Actual goal usage will be recorded at handoff. No phase is accepted merely because its allocation is consumed.

At the first measured checkpoint (409 elapsed seconds), the goal tool reported 274,838 aggregate tokens used. This exceeds the original planning estimate. All workers were instructed to narrow to the current implementation/validation gate and avoid additional broad research. The tool does not provide a per-worker enforced cap for this existing goal; the 120,000 figure must not be presented as a consumption ceiling or actual use.

At the latest measured checkpoint (2,380 elapsed seconds), the goal tool reported **1,025,871 aggregate tokens used**. The original estimate was substantially too low and is superseded for forecasting. Work continues in bounded implementation slices with concise worker context and explicit validation gates; there is no enforced token ceiling on the active goal.

At the next measured checkpoint (4,538 elapsed seconds), the goal tool reported **2,062,218 aggregate tokens used**. This is actual reported usage, not the planning budget. The continuing P3/P5 assignments are bounded to specific parser, recovery and manifest gates; earlier phase success does not complete physical-device, cloud or release gates.

At the 6,056-second checkpoint, the tool reported **3,014,053 aggregate tokens used**. Soft worker allocations are not enforced or equivalent to this aggregate accounting; the original 120,000 estimate is not an actual consumption limit. P5 native implementation and remaining machine-verifiable release work continue under the active goal.

At the 7,623-second checkpoint, the tool reported **3,849,477 aggregate tokens used**. P5 manual provider slices are in adversarial native validation; optional background scheduling and P6 native performance/design work remain separate bounded follow-ups. There is still no enforced token ceiling.

## P6 integration

Native icons, privacy manifests, display checks and native scale measurements are implemented. Measured bottlenecks led to serialized iOS archive/write preparation and Android wrapped row keys plus linear strict JSON scanning. Final independent review found and closed two Android key-validation edge cases and one iOS stale completion callback. iOS passed its full 60-test functional gate and five focused tests after the final callback fix (61 distinct normal tests across those runs). Android 17 passed 31 of 34 discovered tests, with three separately executed process phases intentionally skipped; Android 8 passed 29 of 34 discovered tests with five explicit opt-in/process skips. Current native performance samples are documented in [PERFORMANCE.md](PERFORMANCE.md). Unfiltered iOS accessibility diagnostics remain unresolved and separate from passing functional/display journeys; physical-device acceptance is still open.

The old web README is archived under `docs/legacy/`; the repository entry point now describes native development. The new guarded [packaging command](NATIVE_PACKAGING.md) builds an isolated source snapshot and never uploads or provisions. It distinguishes Android upload and installed-app signing certificates and requires observed artifact version/build equality. No signed distribution artifact has been produced.

The clean review worktree is `/Users/sarathfrancis/work/git/Personal/penny-offline-review`, on `codex/penny-offline-native` from `bcdd69d`. It excludes the user's unrelated Flutter/API changes. Clean install, API typecheck/build plus 150 tests, all 226 repository tests, docs freshness/lint plus 104 tests, web typecheck/production build, zero-finding npm audit, and 67 shared Node plus 19 Python gate groups passed locally. The web build used synthetic configuration and the existing explicit build-only Admin fallback. Final native sources are synchronized into the review worktree, with generated docs regenerated against each worktree's own retained API source. The review commit and hosted checks are separate from native release eligibility.

At the 10,381-second checkpoint, the goal tool reported **5,059,160 aggregate tokens used**. This exceeds the original soft estimate; there is no enforced token ceiling. P6 allocations remain bounded checkpoints and must not be reported as actual usage limits.

## Local demo

The latest normal iOS development build is installed and running in the fresh `Penny Offline Demo` iPhone 17 simulator (iOS 26.4.1), version 3.0.0/build 1, bundle `ca.penny.offline.dev`. It was launched with no test arguments and starts with an empty local vault. No existing simulator or shipping app was erased. Source/binary hashes are in `apps/ios/.build/p6-source-freeze.json`; launch evidence and the actual screen are in ignored `artifacts/offline/demo-device.json` and `ios-demo-launch.png`.

Final iOS visual journeys passed on iPhone 17, iPhone 16e and iPad mini, including large text/dark landscape. The separate unfiltered accessibility diagnostic failed with **13 findings: 10 Dynamic Type, 2 clipping and 1 contrast**. No category was suppressed. Functional/display success does not close that diagnostic or physical VoiceOver acceptance.

Compact machine-readable iOS functional, final-fix, performance and compiled privacy reports are retained in [evidence/](evidence/README.md) for review. Full local result bundles remain in ignored build directories.

### Final minimum-runtime and receipt corrections

Android 8/API26 full regression passed **29 of 34 discovered tests**, with five explicit opt-in/process skips and no failures. It exposed and corrected missing Closeable compatibility and an older Keystore provider's lost large-cipher output; bounded 16 KiB authenticated legacy decryption now requires complete output before returning. Final Android 17 regression passed 31 of 34 discovered tests, with three separately exercised process phases intentionally skipped and no failures;25 JVM tests and lint with zero errors/11 warnings passed. Final fresh-process UI recovery passed with PID 4651 to 4701; 10k recovery passed with PID 4847 to 4909 in 1.563 seconds. The normal development APK passed 16 KiB alignment and was installed into the previously absent `.dev` package on the isolated Android 17 emulator.

The shared PNG corpus exposed seven malformed images admitted by ImageIO, so both native apps now check complete PNG/zlib/scanline integrity before decoding or preparation conversion. The frozen corpus has **24 cases: five valid and 19 invalid**. Final iOS acceptance passed **55 unit plus three affected UI tests (58/58)**, then the expanded corpus passed its focused test. Original failing evidence is preserved. Normal demo and unsigned device builds were rebuilt; iOS frozen source hashes match. Saved receipt caps and portable versions are unchanged. The current iOS demo process was relaunched from that corrected build without test arguments.

The normal Android development app (`ca.penny.offline.dev`) is also installed and running on the Android 17/16KiB emulator, with an empty local vault and no Drive client configuration. Compact [iOS launch](evidence/ios-demo-launch.png) and [Android launch](evidence/android-demo-launch.png) captures show the actual normal apps; neither uses test launch arguments or public test recovery keys.

At the 14,545-second integration checkpoint, the goal tool reported **6,760,360 aggregate tokens used**. This substantially exceeds the original 120,000 soft estimate and is not budget compliance. The tool reports aggregate usage without an enforced per-worker ceiling; final delivery accounting remains separate from phase estimates.

## Review publication and hosted checks

Draft [PR44](https://github.com/sarathfrancis90/penny/pull/44) contains the native development milestone on `codex/penny-offline-native`. Initial implementation commit is `8a3a777`; `bf9a53d` fixes the retained pre-push hook's Git environment when Flutter probes its separate SDK repository. The complete **290-test legacy Flutter suite** passed with the hook enabled before push. The clean review worktree contains none of the user's pre-existing API/Flutter source edits.

Hosted native contract and Android compilation/unit/lint/APK jobs passed on `0a1d401` after explicitly installing Android command-line tools. API37 instrumentation passed its 29 enabled tests, then its process helper failed because the runner lacked ripgrep. API26 encountered a SystemUI crash in the generic AVD profile; the workflow now requires a Pixel 2 profile with validated dimensions, density and navigation keys. The process helper now uses portable exact grep checks. Fixed-head hosted revalidation remains required. iOS hosted simulator results remain pending. Retained API, Firebase rules, backend/Flutter/Android compatibility and documentation jobs passed on that head.

Hosted security scanning exposed four retained Ruby dependency advisories and SAST findings. Ruby resolution and targeted tooling hardening are recorded in [COMPATIBILITY_SECURITY.md](COMPATIBILITY_SECURITY.md); hosted revalidation remains required. No merge, native store upload or public rollout occurred.
