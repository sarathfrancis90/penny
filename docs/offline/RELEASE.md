# Penny Offline release context

Verified against store APIs on 2026-09-13, approximately 19:40 UTC. Local sanitized evidence is in ignored `artifacts/offline/app-store-state.json`, `ios-release-context.json` (19:39:57 UTC) and `play-store-state.json` (19:39:25 UTC). These are observed records, not approval to call the new native apps release candidates. P6 is release preparation. Native manual cloud backup and default-off background coordination now pass bounded source and local runtime gates; real-account backup, signed release artifacts and release eligibility remain unverified.

| Platform | Existing identity | Observed state |
| --- | --- | --- |
| iOS | `com.penny.pennyMobile` | App Store Connect app `6761629879`, Penny - AI Expense Tracker; latest returned store version 2.3.5, READY_FOR_SALE |
| iOS internal builds | Same listing | Latest returned build 10014; processing VALID but TestFlight expired. Other returned recent builds also expired |
| Android | `com.penny.penny_mobile` | Internal track 2.3.5 / version code 10014 completed; alpha 2.0.1 / 27 completed; production and beta returned no releases |

The Play inspection used an uncommitted edit to read tracks/bundles and then deleted it. No release was committed. Do not interpret an empty production track as deletion or as proof of every possible historical release state.

The native development apps currently use separate `ca.penny.offline.dev` identifiers, and Android tests use `.dev.test`. Those identities isolate existing installations but do not prove signed upgrade compatibility. Penny Offline 3.0.0 is an appropriate next major version relative to the observed listings; a release build/version code must exceed the existing 10014 and be refreshed against the store immediately before upload. The checked-in development build number is not a production upload candidate.

## Signing and provider configuration

The local keychain contains valid Apple Development and Apple Distribution identities. The existing App Store Connect key and Play service-account credential are present and authorized the read-only checks. Their values were not reproduced in documentation or logs. Existing credentials do not prove that the new native identities or entitlements are provisioned.

The existing iOS bundle has Apple ID sign-in, in-app purchase and push capabilities. The returned bundle capabilities do not include iCloud. P5 needs an explicitly configured iCloud container, entitlement and matching profile. Native local access does not use the old cloud login or server push.

No `mobile/android/key.properties` file is present. Existing CI signing configuration must be reconciled with the upload certificate before a native release artifact can replace the current listing. Never substitute a newly generated upload identity without verifying Play signing/rotation requirements.

The current physical-device inventory contains an unavailable iPhone 13 Pro Max. There is no verified attached supported device for Foundation Models or Gemini Nano. Local simulator/emulator tests and implementation continue independently; hardware AI, signed upgrade, real-account cloud recovery and store internal-test installation remain external evidence gates.

## Release sequence

P6 preserves capture, finance, portable backup and cloud coordination while adding reviewed storage/worker optimizations and native display/capacity work. iOS records a full 60/60 functional checkpoint, a 5/5 restore-action correction, then all 55 unit tests plus three affected UI journeys (58/58) after the PNG admission fix and a final 24-case corpus method (1/1); each run has zero failures/skips. Android's final API 37/16 KiB run records 34 discovered / 31 passed / three explicit external-process skips. Minimum API 26 records 29/34 passes with five stated opt-in/process skips, followed by the final corpus method 1/1. Its 25 JVM tests pass; lint has zero errors/11 warnings. Final external process checks preserve the UI expense and 10k-record vault. Mandatory native cloud captures preserve all eight record arrays across readers. See [REVIEW.md](REVIEW.md) for exact evidence scope. These local results do not establish physical-device, final release-binary or real-provider behavior; the separate unfiltered iOS accessibility audit remains failing.

| Next gate | Concrete machine-verifiable evidence | Current boundary |
| --- | --- | --- |
| P5/P5b protocol and background integration | Both ports consume shared manifest crypto/invalid fixtures. Native tests exercise account changes at suspension points, cancellation, bad download/authentication, failed publication/readback, interleaved writers and clean-install metadata recovery. Capture native outputs with hashes and cross-reader checks. | Bounded source and native regression gates closed, including durable stop markers, atomic checkpoints and mandatory cross-reader captures. Scheduling is default off, requires configured providers, and has no remote deletion. Fake transports and clocks cannot prove a real provider or OS wake. |
| Final source regression | From the intended source state: `npm run offline:check`, `npm run offline:ios:check`, Android JVM/lint/build and device tests, plus the separate process-persistence script. Record authoritative XML/xcresult counts and source/dependency digests. | P6 evidence and final review corrections are tracked in [REVIEW.md](REVIEW.md). The native workflow now runs on every main PR/push and has an always-running aggregate required gate. Hosted execution, including Android API 26 and API 37/16 KiB, remains separate from host success. |
| Privacy and artifact inventory | Resolve all Android SDK dependencies, inspect merged permissions/network policy; generate iOS archive privacy report and inventory required-reason APIs/manifests. Verify camera strings, no advertising ID, no test-only hooks/keys/fixtures or unintended telemetry configuration. | [PRIVACY.md](PRIVACY.md) is an internal draft. The unsigned iPhoneOS build contains the native icon and file-metadata privacy reasons C617.1/3B52.1. Android records resolved dependencies and platform network policy. Final signed archive reports and ML Kit/AICore traffic/disclosures remain open. |
| Release artifact preflight | Run `npm run offline:release:preflight -- --help`, then inspect the exact exported iOS `.ipa` payload and signed Android `.apk` using independently verified team/container/certificate and a freshly read store maximum. Require matching production identity/version, release signing, no debugger, expected backup/provider settings and recorded hashes. | The root-owned checker is preparation tooling, not a signing or upload action. Development IDs/build 1 and simulator products are not candidates. An APK check does not establish the AAB uploaded to Play. |
| Migration and signed upgrade | Reconcile authenticated source counts/IDs/history against converted records and quarantine totals; preserve source/export evidence. Install the native candidate as an update over an actual signed existing Flutter install or validated bridge. Reopen, compare every supported domain/receipt and test recovery with the old release no longer running. | Offline converter success proves represented input records only. Older absent/null `expenseType` documents, changing pagination, complete savings history, profile/preferences and unsupported history remain source gates. Separate dev bundle tests cannot prove upgrade migration. |
| Native capacity and durability | Measure cold open/save/report/CSV/export/restore at accepted record/receipt/byte bounds, plus cancellation, low storage and injected failed writes. Repeat interruption/cold launch and old-generation recovery on supported hardware. | Native 1k/10k expense and 8 MiB receipt measurements now accompany the Node reference. iOS moves validation/crypto to a serial worker with a guarded final commit; Android batches software row encryption under a Keystore-wrapped key and scans JSON numbers linearly. These synthetic simulator/emulator measurements are not physical-device or larger-vault acceptance. Inline limits remain 2 MiB each / 8 MiB total / 100 receipts. |
| Real provider recovery | In configured signed builds, publish synthetic full-domain data, receive server completion, fetch snapshot and manifest again, authenticate them and restore from only the saved key on a clean installation. Exercise account switch, revocation, offline retry, quota failures, partial objects, two writers and listing capacity. | CloudKit private database/CKAsset and Drive appData adapters are implemented behind configuration gates. Files/SAF export readback alone cannot close this gate. No automatic remote deletion is approved by the P5 contract. |
| Store artifact/install | Validate the exact IPA/AAB, record package hashes and signing lineage, then verify store processing and installation through TestFlight/internal Play on intended devices. Compare the installed version/build and exercise recovery after installation. | No native upload or internal installation occurred in this work. This is a later release action, not part of the current preparation slice. |

Release preflight inputs must come from independent account/signing records, not from the candidate binary being checked. See [ARTIFACT_PREFLIGHT.md](ARTIFACT_PREFLIGHT.md) for command arguments, negative development-artifact results and limits; independent review findings are tracked in [REVIEW.md](REVIEW.md). Preserve a reproducible source revision plus any remaining working-tree patch, toolchain versions, dependency locks and build command alongside artifact hashes. Check the actual exported distribution product, including extensions/frameworks, privacy manifests, entitlements and compiled configuration. Exact IPA payload checks now use a bounded private archive snapshot and bind the product hash to packaging. Direct `.app` checks remain narrower. Exact AAB validation, App Store profile acceptance and Play signing/re-signing remain separate gates.

P6 now includes native icons, compiled privacy/platform inventories, bounded display journeys, measured native scale and guarded local packaging tooling. The iOS unfiltered accessibility diagnostic still reports findings; passing functional journeys do not close them. Finish hosted clean CI, signed packaging and archive checks, signed migration/upgrade, real-provider clean-install recovery and the physical/a11y matrix before any release eligibility claim. [NATIVE_PACKAGING.md](NATIVE_PACKAGING.md) describes local packaging; its exported-IPA/APK preflight deliberately leaves exact AAB validation and store processing/delivery open. Upload and public rollout are not performed by this checklist.

## Store content and privacy readiness

Use [PRIVACY.md](PRIVACY.md) as the data inventory and conditional App Store/Play questionnaire draft. Neither a “Data Not Collected” label nor blanket “zero analytics” wording is supported for a network-capable Android release until SDK behavior is resolved. Google documents ML Kit diagnostics/usage metadata independently of on-device image/text processing. The implemented and explicitly probed platform TLS allowlist is defense in depth and cannot prove absence of custom/native or system-service traffic. Observe the final artifact and reconcile actual SDK disclosures. [ML Kit privacy terms](https://developers.google.com/ml-kit/terms).

The existing store identity also serves retained Flutter users. Play's data-safety form covers all currently distributed versions, so native-only source cannot justify replacing the entire package's disclosures. Preserve legacy privacy/account-deletion support until retirement and migration gates pass. Confirm the public policy, publisher/contact, data-processor list, consent/revocation and remote retention wording against the installed candidate. [Play Data safety guidance](https://support.google.com/googleplay/android-developer/answer/10787469).

Prepare final store screenshots, age rating, review notes, support/privacy URLs and encryption/export-compliance answers from the actual release. Native icons and development display captures exist; no store forms were submitted here. iOS targets iOS 26 with Swift 6; Android minSdk is 26 and compile/targetSdk is 37. Local evidence covers API 26 and Android 17/16 KiB arm64 emulators plus APK alignment; hosted and other physical device/ABI compatibility remain separate. HEIC preparation on Android below API 28 explicitly requires JPEG/PNG instead. Verify final compiled values and compatibility against the previously distributed app before selecting supported devices. Restrict generative-AI claims to verified supported hardware; no-model/manual entry must remain complete on unsupported devices.

## Physical and external evidence still needed

Test real camera receipts, HEIC/HDR/orientation, denied/cancelled permission and large font/VoiceOver/TalkBack flows; measure Foundation Models/Gemini Nano latency and failure/manual fallback on supported devices. iOS retains unfiltered accessibility-audit failures separately from functional status; their platform-control and app-owned findings require element-by-element resolution or evidence before acceptance. Android display screenshots and touch journeys do not establish TalkBack completeness. Test physical Keychain/Keystore invalidation and recovery without claiming every key is secure-hardware-backed from simulator/emulator results. Android row encryption uses an in-memory software data key wrapped by Keystore. Verify OS backup/device-transfer exclusions and clarify that externally exported files and immutable cloud generations survive local deletion.

The account/entitlement, upload-certificate, provider, source-reconciliation, physical-device and store-installation gates require their actual environments. Local fake accounts, synthetic clock events, native cross-writer fixtures and valid signing certificates alone cannot substitute for these results. No public release candidate is established until the evidence corresponds to the exact signed artifact.

The retained Flutter release pipeline still builds Flutter. It must not be invoked as if it released the native apps. No native app has been uploaded or publicly released by this work yet.
