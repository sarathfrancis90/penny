# Bounded reassessment of the original 13 Inspector findings

No finding was suppressed. Original issue list: p6-accessibility-findings.txt; extracted original per-issue screenshots: p6-a11y-review/manifest.json. This is a code/evidence assessment, not a passing accessibility audit.

| Original findings | Source | Evidence and disposition |
| --- | --- | --- |
| Three Dynamic Type warnings: empty-state title, explanation and Add an expense | PennyOffline/ExpensesView.swift:58–65 | Semantic title2/body fonts; no size clamp or fixed-height container. Original normal-size screenshot 045D020E-5F51-444F-88AA-08E4E0D6E27A.png shows complete text. It does not establish behavior at every supported size. Independently, the button's measured 145.7×34.3pt hit area was a concrete app-owned defect; corrected with a minimum 44pt label. Focused UI checks pass; rerun Inspector reports actual 145.7×58pt while retaining its Dynamic Type warning. |
| Five Dynamic Type warnings: two report labels, their two currency values and explanatory paragraph | PennyOffline/FinanceView.swift:12–22 and254–256 | Body fonts, unlimited vertical growth and a VStack at accessibility sizes. Original screenshot 6E874D6F-BBB4-4E2B-B8AE-9A71A320E89C.png shows complete normal-size rows. Existing large-type journey scrolls to CSV before capturing, so it does not directly prove these five elements scale correctly. No specific font/layout defect is established; next local proof should capture these rows at actual system category changes, including XXXL and AX5, before changing the component. |
| Dynamic Type warning: Encrypted backup heading | PennyOffline/FinanceView.swift:9; VaultView.swift:28 | Shared header uses headline, semantic label color and vertical fixedSize. No font clamp found. The large-type Vault captures are scrolled past this header; direct system-font-change evidence for this element is still missing. |
| Dynamic Type warning: disabled Restore a backup | PennyOffline/VaultView.swift:57–58 | Native Button and system font, intentionally disabled until a restore key exists. Do not enable invalid restore or change accessibility state merely to clear the warning. Need a targeted default/AX5 comparison in disabled and enabled states; present data does not prove a scaling defect or a framework false positive. |
| Clipping: Search your expenses | PennyOffline/ExpensesView.swift:89 | Native searchable field, no app-owned fixed height. Original element crop 7D6C7DE1-AE24-4AB1-8A19-CA9A48337458.png shows the full placeholder; warning concerns larger sizes not shown by this crop. A shorter prompt is possible but unproven as a fix. Verify actual system AX5 text entry and placeholder rendering before replacing native search. |
| Clipping: ON YOUR DEVICE | PennyOffline/ExpensesView.swift:30–31 | Caption Label has vertical growth enabled. Original element crop 3164A2A0-C60F-451C-A8B5-33BD1D2B184D.png contains the complete text/icon. No visible normal-size clipping; the existing visual journey does not capture the expense header at large sizes. Need direct system AX5 capture; no demonstrated source fix yet. |
| Contrast: Vault capacity | PennyOffline/VaultView.swift:62; FinanceView.swift:9; PennyColors.swift:6 | Source uses semantic label color. Original reported frame y830..870 extends beyond the 844pt screen; full screenshot 5524AC60-5668-4390-AD05-6604B5F109D2.png does not show the heading. Element crop 56EB7198-2AE7-45AC-B8FA-00BA6A504EE8.png is almost entirely background/black, with no readable heading. This is concrete evidence of an offscreen/chrome sampling limitation, not proof of a bad foreground color. Rerun after scrolling this heading fully between the bars; do not globally filter contrast. |

The next locally achievable diagnostic is a bounded actual-system content-size comparison of these exact elements, with explicit scrolling and screenshots, rather than changing semantic fonts speculatively. `simctl ui <device> content_size` exposes all standard and extended categories on this runtime. Restore the initial device setting afterward. Physical VoiceOver remains a separate gate, but these layout comparisons do not require hardware.

After the 44pt change, HostedHarnessAccessibility.xcresult still fails with 13 unfiltered findings (10 Dynamic Type, 2 clipping, 1 contrast). That run follows the synthetic-consent journey and retains its isolated confirmed public test key: the extra confirmed-key label is now flagged for Dynamic Type, while the disabled Restore row receives contrast instead of the previously offscreen capacity heading. This state difference must not be presented as thirteen identical before/after findings. See hosted-harness-accessibility-findings.txt and summary JSON.

## Subsequent actual-system size captures

The [system-size diagnostic report](ios-system-type-diagnostic.json) now preserves the later default/XXXL/AX5 captures and geometry on an isolated iPhone 16e/iOS26.4.1 simulator. The header, search, empty state, report metrics and backup heading scale and wrap in the inspected captures. The three initial diagnostic tests failed at a temporary restore-field selector; their earlier captures remain partial evidence. A corrected restore-only follow-up passed 3/3 with both disabled and enabled assertions. The original system setting was restored and independently read back.

No additional source correction is justified by these captures. AX5's long report explanation spans the viewport and still lacks complete scrolling visual acceptance; a visible capacity heading has no measured contrast ratio or fresh fully-visible audit pass. The original 13 unfiltered findings and physical VoiceOver gate remain open. These captures must not be counted as a passing complete accessibility suite.
